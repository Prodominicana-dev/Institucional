import React from "react";

export default function Page({ params }: { params: { section: string } }) {
  return (
    <div className="bg-white w-9/12 h-full rounded-xl border-2 border-gray-200"></div>
  );
}
