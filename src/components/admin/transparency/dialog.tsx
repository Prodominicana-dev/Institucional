import React from "react";
import {
  Button,
  Dialog,
  DialogHeader,
  DialogBody,
  DialogFooter,
  Input,
} from "@material-tailwind/react";

export default function TransparencyDialog({
  open,
  handleOpen,
}: {
  open: boolean;
  handleOpen: () => void;
}) {
  return <></>;
}
